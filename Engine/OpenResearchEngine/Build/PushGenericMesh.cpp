#include "../EngineApp.h"

void EngineApp::PushGenericMesh()
{
    // Procedural shape geometry (box, sphere, etc.) is no longer used by the
    // new mesh system.  This function is kept as a stub so BuildScene() can
    // remain unchanged until a future clean-up pass.
}
